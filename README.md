# CasePracticeKT
### A repository implementing the computer game "Buckshot Roulette." It was created as part of a Kotlin homework assignment.
## Launch
1. To launch the GUI version of the application: `./gradlew run`
2. To launch the console version: run the `main` class inside `AppLauncher.kt`
3. To run tests: `./gradlew test`